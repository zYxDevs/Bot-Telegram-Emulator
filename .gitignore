# Secrets — JANGAN PERNAH masuk ke repo
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Data runtime bot (history & community KB) — kecuali KB curated
data/*
!data/kb/

# Logs
*.log
npm-debug.log*

# Editor / OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db

# yt-dlp cookies
yt-cookies.txt

# Local API key rotation store + env backups (swap-copux.sh) — chmod 600, never commit
.copux-keys.json
.env-backups/

__pycache__/
*.bak.*
_harness_once.js

# Scratch test/benchmark artifacts — never commit
test_*.py
test_*.html
benchmark_*.py
*_test.html
download/
copux_logs.txt

# AI agent local config (berisi token/allowlist lokal) — JANGAN PERNAH masuk ke repo
.claude/

# Timestamped backups (mis. bot.js.bak-YYYYMMDD-HHMMSS)
*.bak-*